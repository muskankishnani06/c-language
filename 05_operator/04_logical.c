#include <stdio.h>

int main()
{

    int a = 0;
    int b = 1;

    // && and operator;

    printf("\n %d", a && b);

    // || or operator;

    printf("\n %d", a || b);

    //   not operator;

    printf("\n %d", !a);
     printf("\n %d", !b);

    return 0;
}