#include <stdio.h>

int main()
{

    int numArray[5] = {10, 20, 30, 40, 50};

    printf("enter element for modification:-");
    scanf("%d", &numArray);

    printf(" \n After modification %d", numArray[0]);

    return 0;
}